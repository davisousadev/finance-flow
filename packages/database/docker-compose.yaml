services:
  database:
    image: postgres:15
    container_name: finance-flow-database
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: 123456
      POSTGRES_DB: finance_flow
    ports:
      - "5432:5432"
    volumes:
      - ./data:/var/lib/postgresql/data